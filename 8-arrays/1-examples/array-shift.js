const animals = ["monkey", "dog", "cat", "koala", "elephant"];

// unshift it used to add the first element of the array

animals.unshift("llama");

console.log(animals);

// shift method it used to remove the first element of the array list.

animals.shift();
animals.shift();
animals.shift();

console.log(animals);
