const animals = ["monkey", "dog", "cat", "koala", "elephant"];

// when we wanna to add the array list the item in the last we use the "push" method

animals.push("cow");

console.log(animals);

// the "pop" method used to remove the last item

animals.pop();
animals.pop();
animals.pop();

console.log(animals);
